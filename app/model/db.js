import r from 'rethinkdb';
import config from 'config';

/**
 * @module For now going to pretty much hax this in but will work on it more
 * the `problem` with RethinkDB is that the api is so fantastic abstracting
 * it away does more harm than good!!
 */

 // This isnt used but will leave this file here for now just in case
 module.exports = r;
